# Task 7 断点恢复与完成后自动关机

- 目标实例：`92xpf851bc-177af1d6`，按量计费。
- 平台 `/usr/bin/shutdown` 是自定义脚本，任何参数都会触发实例关机；探测时曾意外中断评估。
- 评估逐条落盘，数据未损坏；已从 val `2639/3052` 断点续跑，未重算已有结果。
- 已修正 `Dead ???` screen 被误判为运行中的问题。
- 已启动 `static_qlora_shutdown_guard`：仅当 val=`3052`、test=`7596`、summary completed、metrics/failures 齐全、无 `failure_*.json`、评估 screen 已退出时，写 SHA256 清单、`sync` 后调用平台关机。
- 任一异常或数量不符：写 `shutdown_guard_status.json` 并禁止关机。
- 平台固定时间关机已取消，避免评估变慢时被提前中断。
- 关键脚本：`server/evaluation_shutdown_guard.py`、`server/run_evaluation_shutdown_guard.sh`。
- 2026-07-15 本地全量测试：`50 passed`。
