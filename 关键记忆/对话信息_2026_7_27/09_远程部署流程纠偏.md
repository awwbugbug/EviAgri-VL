# 远程部署流程纠偏

- Task11A.3 两次启动问题属于工程部署故障，不属于模型、数据或科学失败；Python/GPU均未启动，结果未损坏。
- 科学版本与工程尝试必须分离：协议变化才递增 `protocol_vN`；权限、路径、引号等只递增 `attempt_NN`。
- Task11A.3 实际映射：formal_v1=attempt_01，formal_v2=attempt_02，formal_v3=attempt_03；三者均属于同一 protocol_v1，有效结果为 attempt_03。
- 启动前一次完成：文件/哈希、`bash -n`、LF、Python环境、输入哈希、空输出目录、重复进程、磁盘/GPU检查。
- Linux shell 默认用 `bash script.sh` 启动；需直接执行时使用 `install -m 755`。禁止因工程故障提升科学版本号。
- 规范：`docs/remote-deployment-runbook.md`。
