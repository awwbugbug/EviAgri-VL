# 数据集传输默认策略

- 今后新增数据集默认先下载到本地，再上传科研服务器。
- 本地下载完成后必须先核对官方文件大小、MD5/SHA256（若官方提供）及压缩包完整性。
- 上传必须使用可断点恢复方式，写入独立 `.partial` 文件；服务器复核通过后再原子命名。
- 禁止覆盖已验证文件，禁止将未校验分片当作正式数据。
- 仅当本地链路不可用，或实测证明服务器直下更稳定且更快时，才单独批准服务器直下。
- 数据集本体、临时分片、凭据均不得提交 Git。
