# 全量数据后台上传启动

- 时间：2026-07-12
- 方式：Windows OpenSSH SFTP 后台传输，顺序串行上传。
- 顺序：IP102 Detection ZIP → Age TAR（断点续传）→ IP102 classification TAR。
- 远端目录：`/root/autodl-tmp/EviAgriDiag/datasets/_incoming`
- 健康检查：后台进程 PID 21236 存活；Detection 远端文件已增长至 67,788,800 bytes；错误日志为空。
- Age 断点：远端已有 22,872,064 bytes，与本地同长前缀 SHA256 完全一致，可安全续传。
- 日志：`artifacts/full_dataset_upload_2026-07-12.{stdout,stderr}.log`
- 硬门槛：三份远端文件逐个 SHA256 验收前，不解包、不启动正式 QLoRA。
- 验收守卫：服务器 `screen` 会话 `full_upload_verify` 已启动；仅在三份文件大小全部达标后计算 SHA256，通过才生成 `full_upload.verified`。
- 守卫测试：成功、哈希不匹配、缺失超时 3 条路径均通过。
- 安全：使用项目专用临时 SSH 授权；不在项目或关键记忆中保存密码/私钥，数据验收后清理该授权。
